## Summary

<!-- What changed and why. Link the issue if there is one. -->

## Test plan

- [ ] `./bin/test` (or unit harness + `./bin/build` if the full gate cannot run here)
- [ ] `./bin/pack` if packaging, versioning, or the public API changed
- [ ] Effect / CLI change: checked completion, not only a matching prefix

## Notes

- Breaking CLI or `TextEffects` change?
- New `PackageReference`? (not allowed in this repo)
